"""
fixed_overlap chunking — STUB (Builder 1)

Fixed token/char size with overlap. Baseline strategy.
"""

from __future__ import annotations


def chunk_text(text: str, size: int = 512, overlap: int = 64) -> list[str]:
    """TODO(Builder 1): implement fixed-size overlapping windows."""
    raise NotImplementedError("fixed_overlap chunking not implemented")
